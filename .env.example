# Create a local .env file from this template.
# Generate a key in Google AI Studio and never commit the real value.
GEMINI_API_KEY=your_google_ai_studio_api_key

# Optional: change this to another Gemini model available to your account.
GEMINI_MODEL=gemini-2.5-flash

